#!/usr/bin/java --source 25 -ea

void main(String []args) {
    int[] is = new int[args.length];
    for (int i = 0; i < args.length; i++) {
        is[i] = Integer.parseInt(args[i]);
    }
    Arrays.sort(is);
    IO.println(Arrays.toString(is));
}