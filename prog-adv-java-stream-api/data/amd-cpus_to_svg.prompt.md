Ich habe die folgenden Daten über die Entwicklung von AMD CPUs [@amd-cpus.json](file:///Users/Michael/Teaching/Lectures/prog-adv-java-streams/data/amd-cpus.json) . Bitte generiere mir daraus ein Haupt SVG und zwei SVGs, die ich als Overlay benutzen kann, die jeweils die folgenden Daten enthalten sollen:
Haupt SVG: Auf der x-Achse: Name der CPU, Modell und "release_date" (Monat.Jahr) - bitte beachte beim Abstand zwischen den Werten die vergangene Zeitspanne. D.h. die x-Achse repräsentiert den Zeitablauf. Y-Achse: "spec_baseline/cores"; d.h. Performance per Core.
1. Overlay SVG (d.h. ein SVG, dass die selbe Größe hat, aber keinen Hintergrund): Y-Achse spec_baseline
2. Overlay SVG: #Cores

Bitte benutze für die Verbindungslinien gestrichelt und gepunktet, sowie eine Kombination davon, und auch kräftige Farben. Die Linke Y-Achse soll die Performance/per Core Achse sein. Im zweiten Schritt wird dies erweitert um die "baseline_Performance". Die Anzahl der Cores wird über eine Y-Achse der rechten Seite abgebildet. Bitte stelle sicher, dass eine Zuordnung zur Achse immer einfach visuell möglich ist. Bitte lass die x-Achse bei 01.2027 (ohne Eintrag) anfangen und bei 12.2026 enden. Für den letzten Eintrag liegen nicht alle Daten vor - dort ist also nur "cores" angegeben. Bitte berücksichtige dies bei der Darstellung.

Bitte generiere die SVGs wie in dem Foliensatz:[@folien.de.rst](file:///Users/Michael/Teaching/Lectures/theo-ds-baeume-und-graphen/folien.de.rst) . D. h. mit komplett relativen Größen.
